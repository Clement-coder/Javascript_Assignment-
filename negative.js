// 1. Write a program that determines whether a given number is positive or negative.

let num = -0;
let name = "Clement";
let lastName = "Raymond";
let fullName = name + " " + lastName;

if (num > 0) {
    console.log(fullName + " is a positive number.");
} else if (num < 0) {
    console.log(fullName + " is a negative number.");
} else {
    console.log(fullName + " the number is zero.");
}